# Cloudflare の本番リソース ID は wrangler.toml に設定します。
# ローカルの UI 開発には不要です。
PUBLIC_SITE_URL=http://localhost:4321
# Worker runtime secrets (RESEND_API_KEY / RESEND_TO) are configured with
# `wrangler secret put`; never place real values in this file.
